{
  "name": "acpbot-website",
  "type": "module",
  "version": "0.3.1",
  "private": true,
  "description": "acpbot.app — landing + docs (Astro)",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npx wrangler pages deploy",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.13.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
