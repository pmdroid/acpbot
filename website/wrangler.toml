# Cloudflare Pages (website package as Root directory)
#
# Dashboard settings when Root directory = website:
#   Build command:  bun run build
#   Deploy command: npx wrangler pages deploy
#   (do NOT use `npx wrangler deploy` — that is for Workers)

name = "acpbot"
compatibility_date = "2025-08-01"
pages_build_output_dir = "dist"
